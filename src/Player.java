import java.awt.event.KeyEvent;

// AP CS Project
// Alex, Johnny, Ethan, and Uday
//
// This is a template comment that you should paste verbatim above every class.
// Fill out any necessary information:
//
// File: Player.java
//
// Add your name here if you work on this class:
/** @authors Ethan */ 
//public class Player extends Entity {
//	public static String ID = "Player"
//	public static String TYPE = "Player"
//}
/*
 * Entity en = entities.get(i);
			if (keysDown.contains(KeyEvent.VK_UP)) {
				if (en.getY() > 0) {
					en.updateYBy(-2);
				}
			} else if (keysDown.contains(KeyEvent.VK_DOWN)) {
				if (en.getY() + en.getSize().getHeight() < size.getHeight()) {
					en.updateYBy(2);
				}
			}
			if (keysDown.contains(KeyEvent.VK_LEFT)) {
				if (en.getX() > 0) {
					en.updateXBy(-2);
				}
			} else if (keysDown.contains(KeyEvent.VK_RIGHT)) {
				if (en.getX() + en.getSize().getWidth() < size.getWidth()) {
					en.updateXBy(2);
				}
			}
 * */
 